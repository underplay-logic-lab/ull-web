# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# python (scripts/modal_wan_animate.py)
__pycache__/
*.pyc

# supabase CLI local machine linkage (not portable)
supabase/.temp/

# typescript
*.tsbuildinfo
next-env.d.ts

# local build / large binary artifacts (not tracked in git — rebuild or fetch out of band)
ai_toolkit-*.whl
/public/downloads/

# `modal run modal_angle_worker.py` local CLI output
/angle_out/
/angle_bench/
/upscale_out/
/upscale_bench/

# TRELLIS / angle worker local CLI output
trellis_out/
angle_test_tuned/
angle_test_tuned2/
